[server]
# Position from top of screen (0.0 = top, 0.5 = center, 1.0 = bottom)
top_margin = 0.12

# Show percentage in OSD
show_percentage = true

# Clamp the volume OSD/slider to 100%
max_volume = 100
